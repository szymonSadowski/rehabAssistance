import cv2
import numpy as np
import time
import pointsDictionary
# img = np.zeros((200, 300, 3), np.uint8)
# img[:] = (255, 255, 255)
# cv2.circle(img, (1, 2), radius=0, color=(0, 0, 255), thickness=4)
# cv2.circle(img, (6, 8), radius=0, color=(0, 0, 255), thickness=4)
# start_time = time.time()
# cv2.imshow('img', img)
#
# cv2.waitKey(3000)
#
# img[:] = (255, 255, 255)
# cv2.imshow('img', img)
#
# print("NO I KONIEC")
# cv2.waitKey(0)

print(pointsDictionary.points_dictionary(1))